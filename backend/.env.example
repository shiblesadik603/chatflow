# --- Server ---
NODE_ENV=development
PORT=5001

# --- Frontend origin (for CORS) ---
CLIENT_URL=http://localhost:5173

# --- Database ---
MONGO_URI=mongodb://127.0.0.1:27017/chatflow

# More variables (JWT secrets, REDIS_URL, ...) will be added in later
# phases as each feature is introduced.
