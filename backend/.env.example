# --- Server ---
NODE_ENV=development
PORT=5001

# --- Frontend origin (for CORS) ---
CLIENT_URL=http://localhost:5173

# More variables (MONGO_URI, JWT secrets, REDIS_URL, ...) will be added
# in later phases as each feature is introduced.
